import { ConvertirDiaATextoPipe } from './convertir-dia-atexto.pipe';

describe('ConvertirDiaATextoPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirDiaATextoPipe();
    expect(pipe).toBeTruthy();
  });
});
