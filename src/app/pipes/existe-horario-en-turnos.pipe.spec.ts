import { ExisteHorarioEnTurnosPipe } from './existe-horario-en-turnos.pipe';

describe('ExisteHorarioEnTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new ExisteHorarioEnTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
